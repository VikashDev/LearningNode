//These are global which are accessible from everwhere
var path = require("path");

//var hello = "hello world";

//var justNode = hello.slice(5);

console.log(`hello ${path.basename(__filename)}`);

//console.log(__dirname);  Show the full file dir

//console.log(__filename);  Show the full path with filename